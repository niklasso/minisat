name: Build and Test

on: [push]

jobs:
  Linux:

    runs-on: ubuntu-18.04
    
    steps:
    - name: Install Packages
      run: sudo apt-get install zlib1g-dev make cmake

    - uses: actions/checkout@v1

    - name: mergesat build
      run: make cd cp cr csh d r p sh lr ld lp lsh

